<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://7suyash7.github.io/portfolio/feed.xml" rel="self" type="application/atom+xml"/><link href="https://7suyash7.github.io/portfolio/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-16T16:03:11+00:00</updated><id>https://7suyash7.github.io/portfolio/feed.xml</id><title type="html">blank</title><subtitle>Suyash Nayan is a systems and protocol engineer moving from Ethereum execution infrastructure into AI inference, ML systems, and performance-critical backend engineering. </subtitle><entry><title type="html">Building FedAvg From Scratch in PyTorch</title><link href="https://7suyash7.github.io/portfolio/blog/2026/building-fedavg-from-scratch-in-pytorch/" rel="alternate" type="text/html" title="Building FedAvg From Scratch in PyTorch"/><published>2026-07-16T00:00:00+00:00</published><updated>2026-07-16T00:00:00+00:00</updated><id>https://7suyash7.github.io/portfolio/blog/2026/building-fedavg-from-scratch-in-pytorch</id><content type="html" xml:base="https://7suyash7.github.io/portfolio/blog/2026/building-fedavg-from-scratch-in-pytorch/"><![CDATA[<p>I recently built a small Federated Averaging implementation from scratch in PyTorch. The goal was not to chase benchmark performance, but to understand what actually happens inside FedAvg: how the server initializes a model, how clients train locally, how their weights are averaged, and how IID vs non-IID client data affects learning.</p> <p>The setup was intentionally simple. I used a tiny synthetic classification dataset and a two-layer MLP classifier. The model takes an input feature vector, passes it through a hidden layer with a ReLU activation, and outputs raw logits for each class. I deliberately kept the model small because the interesting part of this project was not the architecture. The interesting part was the training system around it.</p> <p>The basic pipeline looked like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Build synthetic dataset
-&gt; split into train/test
-&gt; partition train data across clients
-&gt; initialize global model
-&gt; select clients each round
-&gt; train selected clients locally
-&gt; aggregate client models with sample-weighted averaging
-&gt; evaluate global model
</code></pre></div></div> <p>The most important thing I learned is that FedAvg is not conceptually complicated, but it is very easy to implement incorrectly in subtle ways.</p> <p>In normal centralized training, there is one model and one dataset. You shuffle the data into batches, compute loss, backpropagate, and update the model. In FedAvg, every selected client gets a copy of the same global model, trains that copy on its own local data, and sends the updated weights back to the server. The server then creates a new global model by averaging those client weights.</p> <p>So instead of thinking:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>one model trains on all data
</code></pre></div></div> <p>FedAvg is more like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>many client models start from the same global weights
each trains locally
the server averages their final parameters
</code></pre></div></div> <p>That distinction mattered a lot. One bug I had to avoid was accidentally training multiple clients on the same model object. That would make client updates sequential instead of independent, which breaks the whole “train locally, then average” structure. Each client needs a fresh model loaded with the same global state at the start of the round.</p> <p>Another thing I paid attention to was how model state is copied. PyTorch <code class="language-plaintext highlighter-rouge">state_dict</code>s are dictionaries of tensors, but blindly passing them around can create shared references. For FedAvg, the server needs safe snapshots of model parameters. That means cloning and detaching tensors so later optimizer updates do not mutate a state that was supposed to be frozen.</p> <h2 id="sample-weighted-aggregation">Sample-Weighted Aggregation</h2> <p>The aggregation step was a useful systems lesson. FedAvg does not simply average clients equally unless every client has the same amount of data. The correct update is sample-weighted:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>new_global = sum((client_samples / total_samples) * client_state)
</code></pre></div></div> <p>This means a client with 100 examples should influence the new global model more than a client with 10 examples. Implementing this forced me to treat a model not as one object, but as a collection of named tensors: <code class="language-plaintext highlighter-rouge">fc1.weight</code>, <code class="language-plaintext highlighter-rouge">fc1.bias</code>, <code class="language-plaintext highlighter-rouge">fc2.weight</code>, and <code class="language-plaintext highlighter-rouge">fc2.bias</code>. Aggregation is just tensor math applied key-by-key across matching state dictionaries.</p> <h2 id="iid-vs-non-iid-clients">IID vs Non-IID Clients</h2> <p>The IID vs non-IID data split was probably the most interesting part. In the IID case, each client gets a roughly representative slice of the global dataset. In the non-IID case, I sorted examples by label, split them into shards, and assigned only a few shards to each client. That creates label-skewed clients where each client may only see a small part of the label space.</p> <p>This helped make the main FedAvg problem more concrete. With IID data, clients usually produce updates that point in somewhat compatible directions. With non-IID data, one client might mostly learn class 0, another might mostly learn class 1, and another might mostly learn class 2. When the server averages these models, the result can be noisier or worse than the IID case. This is the client drift problem: local training can pull each client model toward its own biased data distribution.</p> <p>I also experimented with two important knobs:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>local_epochs: how much each client trains before aggregation
client_fraction: what fraction of clients participate each round
</code></pre></div></div> <p>Increasing local epochs means each client does more work per communication round. This can reduce the number of server rounds needed, but it can also make client drift worse, especially when the data is non-IID. Increasing the client fraction means the server averages more client updates per round, which usually gives a more representative global update, but costs more communication.</p> <h2 id="details-that-mattered">Details That Mattered</h2> <p>The biggest implementation details I had to get right were:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use one shared permutation when shuffling features and labels.
Never apply softmax before cross-entropy loss.
Reset gradients before each optimizer step.
Clone and detach model state before storing it.
Load the same global state into every selected client.
Aggregate client states with sample-count weighting.
Use fixed seeds so experiments are reproducible.
</code></pre></div></div> <p>The softmax point was a good reminder. The model returns raw logits, and <code class="language-plaintext highlighter-rouge">CrossEntropyLoss</code> handles the softmax/log-softmax internally. Adding softmax manually would double-normalize the outputs and damage the gradients.</p> <p>The seed handling was also more important than I expected. For controlled experiments, I used the same seed across runs when comparing client fractions or local epoch counts. That way, the only thing changing was the variable being tested. Otherwise, randomness in initialization, client selection, or batch ordering could make the comparison noisy.</p> <h2 id="what-clicked">What Clicked</h2> <p>Overall, this project helped me see FedAvg as a simple but powerful distributed training pattern:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>broadcast weights
train locally
average weights
repeat
</code></pre></div></div> <p>The hard part is not the high-level idea. The hard part is preserving the right invariants: clients must start from the same global model, data-label pairs must stay aligned, state dictionaries must not accidentally share storage, and aggregation must respect how much data each client trained on.</p> <p>This was a good from-scratch project because it made federated learning feel less abstract. FedAvg is basically ordinary SGD running on many local datasets, plus careful parameter averaging on the server. Once I implemented the pieces myself, the tradeoffs around IID vs non-IID data, local epochs, client sampling, and communication cost became much easier to reason about.</p>]]></content><author><name></name></author><category term="learning-notes"/><category term="federated-learning"/><category term="pytorch"/><category term="fedavg"/><category term="distributed-training"/><category term="ai-systems"/><summary type="html"><![CDATA[A learning note from implementing Federated Averaging in PyTorch, with client-local training, sample-weighted aggregation, and IID vs non-IID data splits.]]></summary></entry><entry><title type="html">LoRA Fine-Tuning a Tiny Chat Model with Unsloth</title><link href="https://7suyash7.github.io/portfolio/blog/2026/lora-fine-tuning-a-tiny-chat-model-with-unsloth/" rel="alternate" type="text/html" title="LoRA Fine-Tuning a Tiny Chat Model with Unsloth"/><published>2026-07-08T00:00:00+00:00</published><updated>2026-07-08T00:00:00+00:00</updated><id>https://7suyash7.github.io/portfolio/blog/2026/lora-fine-tuning-a-tiny-chat-model-with-unsloth</id><content type="html" xml:base="https://7suyash7.github.io/portfolio/blog/2026/lora-fine-tuning-a-tiny-chat-model-with-unsloth/"><![CDATA[<p>I recently worked through a small end-to-end fine-tuning project using LoRA, Unsloth, TRL, and a 4-bit Qwen2.5-0.5B chat model.</p> <p>The goal was not to produce a powerful model. The point was to understand the full supervised fine-tuning pipeline from the inside: loading a quantized base model, attaching LoRA adapters, formatting a tiny instruction dataset, running a short SFT training job, and generating a response from the tuned model.</p> <p>This was useful because it connected a few concepts that can feel abstract when reading about LLM fine-tuning. I got to see how 4-bit quantization reduces memory usage by storing large model weights in a compressed representation, while LoRA keeps the base model frozen and trains only small adapter matrices inside selected projection layers such as <code class="language-plaintext highlighter-rouge">q_proj</code>, <code class="language-plaintext highlighter-rouge">k_proj</code>, <code class="language-plaintext highlighter-rouge">v_proj</code>, and <code class="language-plaintext highlighter-rouge">o_proj</code>.</p> <p>It also made the data side feel less mysterious. The model does not train on dictionaries or “prompts” directly. Each instruction/response pair has to be formatted into a consistent text template, tokenized into integer IDs, wrapped in a dataset, and passed into an SFT trainer. Seeing that flow end to end made training feel less like magic and more like a sequence of explicit engineering choices.</p> <h2 id="what-i-built">What I Built</h2> <ul> <li>Loaded a 4-bit quantized Qwen2.5 chat model with Unsloth.</li> <li>Verified that the model was using bitsandbytes 4-bit linear layers.</li> <li>Added LoRA adapters to attention projection modules.</li> <li>Counted total vs trainable parameters to understand the savings from PEFT.</li> <li>Built and formatted a tiny instruction dataset.</li> <li>Tokenized examples and checked sequence lengths.</li> <li>Configured a lightweight SFT training run.</li> <li>Switched the model into inference mode and generated a deterministic reply.</li> </ul> <h2 id="what-clicked">What Clicked</h2> <p>The biggest takeaway was that fine-tuning is not just “call <code class="language-plaintext highlighter-rouge">train()</code> on a model.” It is a sequence of careful decisions around model loading, quantization, adapter placement, tokenizer behavior, dataset formatting, training configuration, and inference setup.</p> <p>Even though the training run was intentionally tiny, the project gave me a clearer mental model for how modern LLM fine-tuning pipelines are put together. It also gave me another small bridge from systems engineering into AI infrastructure: not just using model APIs, but understanding the machinery around memory, parameters, data formatting, training loops, and inference behavior.</p> <p>Repo: <a href="https://github.com/7suyash7/lora-fine-tune-a-tiny-chat-model-with-unsloth">7suyash7/lora-fine-tune-a-tiny-chat-model-with-unsloth</a></p>]]></content><author><name></name></author><category term="learning-notes"/><category term="llm-fine-tuning"/><category term="lora"/><category term="unsloth"/><category term="qwen"/><category term="ai-systems"/><summary type="html"><![CDATA[A learning note from an end-to-end LoRA fine-tuning exercise with Unsloth, TRL, PEFT, and a 4-bit Qwen2.5-0.5B chat model.]]></summary></entry><entry><title type="html">Building a Multi-Protocol Arbitrage Bot</title><link href="https://7suyash7.github.io/portfolio/blog/2025/building-a-multi-protocol-arbitrage-bot/" rel="alternate" type="text/html" title="Building a Multi-Protocol Arbitrage Bot"/><published>2025-10-28T00:00:00+00:00</published><updated>2025-10-28T00:00:00+00:00</updated><id>https://7suyash7.github.io/portfolio/blog/2025/building-a-multi-protocol-arbitrage-bot</id><content type="html" xml:base="https://7suyash7.github.io/portfolio/blog/2025/building-a-multi-protocol-arbitrage-bot/"><![CDATA[<p>External technical deep dive hosted on Medium.</p>]]></content><author><name></name></author><category term="systems-notes"/><category term="rust"/><category term="defi"/><category term="graph-algorithms"/><category term="amm-math"/><category term="performance"/><summary type="html"><![CDATA[A technical deep dive into ArbRs, a Rust engine for Uniswap, Curve, Balancer-style AMM math, graph search, and net-profit-aware arbitrage modeling.]]></summary></entry><entry><title type="html">Observing Ethereum’s Mempool Directly with Reth</title><link href="https://7suyash7.github.io/portfolio/blog/2025/observing-ethereums-mempool-directly-with-reth/" rel="alternate" type="text/html" title="Observing Ethereum’s Mempool Directly with Reth"/><published>2025-04-14T00:00:00+00:00</published><updated>2025-04-14T00:00:00+00:00</updated><id>https://7suyash7.github.io/portfolio/blog/2025/observing-ethereums-mempool-directly-with-reth</id><content type="html" xml:base="https://7suyash7.github.io/portfolio/blog/2025/observing-ethereums-mempool-directly-with-reth/"><![CDATA[<p>External technical deep dive hosted on Medium.</p>]]></content><author><name></name></author><category term="systems-notes"/><category term="rust"/><category term="ethereum"/><category term="p2p-networking"/><category term="reth"/><category term="observability"/><summary type="html"><![CDATA[A Rust/Reth deep dive on building a standalone Ethereum P2P mempool monitor without relying on a full node or external RPC abstraction.]]></summary></entry></feed>